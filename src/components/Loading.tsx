const Loading = () => {
    return (
            <img src="../../public/loading.png"
                 alt=""
                 className="w-[50px] animate-spin"
            />
    );
};

export default Loading;